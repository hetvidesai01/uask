services:
  postgres:
    image: postgres:16
    restart: unless-stopped
    ports:
      - "5432:5432"
    environment:
      POSTGRES_DB: uask
      POSTGRES_USER: uask
      POSTGRES_PASSWORD: uask
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
