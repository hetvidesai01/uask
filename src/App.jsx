function App() {
  return (
    <div className="container stack" style={{ paddingBlock: 'var(--sp-8)' }}>
      <h1 style={{ fontFamily: 'var(--font-display)' }}>UASK</h1>
      <p style={{ color: 'var(--c-text-mute)' }}>
        Phase 1 setup complete — routing and pages come next.
      </p>
    </div>
  )
}

export default App
